{
  "name": "incident-room",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run test && npm run build",
    "check:lab": "wrangler versions upload --dry-run -c lab-workers/checkout/wrangler.jsonc --var CHECKOUT_BEHAVIOR:broken && wrangler versions upload --dry-run -c lab-workers/checkout/wrangler.jsonc --var CHECKOUT_BEHAVIOR:healthy && wrangler versions upload --dry-run -c lab-workers/checkout/wrangler.jsonc --var CHECKOUT_BEHAVIOR:concurrent && wrangler versions upload --dry-run -c lab-workers/payment/wrangler.jsonc",
    "dev:worker": "npm run build && wrangler dev",
    "deploy": "npm run check && wrangler deploy --keep-vars"
  },
  "dependencies": {
    "lucide-react": "^1.37.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.1.1",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  }
}
